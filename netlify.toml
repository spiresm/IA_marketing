[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # S'assure que toutes les fonctions ET le fichier profil.json (à la racine) sont inclus
  included_files = ["netlify/functions/**", "profil.json"]

[functions.proxy]
  external_node_modules = ["node-fetch"]

# Nouvelle section pour votre fonction getProfils.js
[functions.getprofils]
  external_node_modules = ["@octokit/rest"] # getProfils.js a besoin de @octokit/rest

[functions.uploadImage]
  external_node_modules = ["@octokit/rest", "node-fetch"]

[functions.sendRequest]
  external_node_modules = ["nodemailer", "multiparty"]
