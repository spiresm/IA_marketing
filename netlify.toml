# ... (votre section [build] et [functions] générale)

[functions.proxy]
  external_node_modules = ["node-fetch"]

# Temporairement, supprimez ou commentez la ligne external_node_modules pour getProfils
[functions.getProfils] # Assurez-vous que le P est majuscule ici
  # external_node_modules = ["@octokit/rest"] # <-- Cette ligne DOIT être commentée ou supprimée

[functions.uploadImage]
  external_node_modules = ["@octokit/rest", "node-fetch"]

[functions.sendRequest]
  external_node_modules = ["nodemailer", "multiparty"]

# ... (le reste de votre fichier netlify.toml)
