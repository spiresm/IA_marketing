[build]
  publish = "."
  functions = "netlify/functions" # <-- Gardez cette ligne ! Elle est la bonne.

[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/**"]
  # directory = "netlify/functions" # <-- SUPPRIMEZ CETTE LIGNE ! Elle est redondante et problématique.

[functions.proxy]
  external_node_modules = ["node-fetch"]

[functions.uploadImage]
  external_node_modules = ["@octokit/rest", "node-fetch"]

[functions.sendRequest]
  external_node_modules = ["nodemailer", "multiparty"]
